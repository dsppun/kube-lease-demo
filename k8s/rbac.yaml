# Apply this BEFORE the Deployment.
# Without it, the pod gets "403 Forbidden" the moment it touches a Lease.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kube-lease-demo
  namespace: default
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kube-lease-demo
  namespace: default
rules:
  - apiGroups: ["coordination.k8s.io"]
    resources: ["leases"]
    verbs: ["get", "list", "watch", "create", "update", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: kube-lease-demo
  namespace: default
subjects:
  - kind: ServiceAccount
    name: kube-lease-demo
    namespace: default
roleRef:
  kind: Role
  name: kube-lease-demo
  apiGroup: rbac.authorization.k8s.io
